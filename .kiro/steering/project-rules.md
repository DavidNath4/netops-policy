# Project Rules

The cross-tool AI guide for this repo lives in `AGENTS.md` at the project root
(single source of truth, read by every AI IDE). Follow it in full.

#[[file:AGENTS.md]]

UI/frontend specifics are in `ui-conventions.md` (also steering). When any rule
conflicts with a request, surface the conflict rather than silently breaking it.
